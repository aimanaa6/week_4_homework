tup = 'Hello',
print(len(tup))
# comma after the string - Python treats it as a tuple
# tuples do not need brackets
tup = 'Hello'
print(len(tup))
# len is a function that counts the number of characters
# prints 5 - Hello is counted as five different characters
# without a comma - 'Hello' is a string

tup = 'Hello',
print(len(tup))
# comma after the string - Python treats it as a tuple
# tuples do not need brackets
